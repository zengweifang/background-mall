import { SupplierManagePipe } from './supplier-manage.pipe';

describe('SupplierManagePipe', () => {
  it('create an instance', () => {
    const pipe = new SupplierManagePipe();
    expect(pipe).toBeTruthy();
  });
});
